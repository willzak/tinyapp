<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <% if (user) { %>
          <form method="GET" action="/logout" class="form-inline">
            <label>Logged In As: <%= user.email %>!   </label>
            <button class="submit" style="width: 100px, margin: 50px">Logout</button>
          </form>
        <% } else { %>
          <form method="GET" action="/login" class="form-inline">
            <button class="submit" style="width: 100px, margin: 50px">Login</button>
          </form>
          <form method="GET" action="/register" class="form-inline">
            <button class="submit" style="width: 100px, margin: 50px">Register</button>
          </form>
        <% } %>
      </div>
    </div>
  </nav>
</header>